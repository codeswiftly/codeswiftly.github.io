# PassKit Raw Reference

@PageImage(purpose: icon, source: "passkit-raw-index-icon.codex", alt: "Placeholder icon")

@Metadata {
    @PageColor(gray)
  @TitleHeading("Review PassKit Raw Reference")
  @PageImage(purpose: icon, source: "track-wallet-icon.codex", alt: "Wallet and PassKit icon")
  @PageImage(purpose: card, source: "passkit-raw-index-card.codex", alt: "PassKit Raw Reference card")
}

@Image(source: "passkit-raw-index-hero.codex", alt: "PassKit Raw Reference hero")

Raw PassKit API mirrors for quick lookup.

## Overview

This catalog includes a large set of raw PassKit doc mirrors under the
`apple-passkit-raw/` directory. They are intentionally not enumerated here.
Use DocC search to jump to the exact symbol or page you need.
