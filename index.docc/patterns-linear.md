# Linear Patterns


@Metadata {
  @TitleHeading("Browse Linear Patterns")
}

Use these for linear scans when the data is a linear sequence and a single pass is the goal.

@Options {
  @TopicsVisualStyle(detailedGrid)
}

@Links(visualStyle: list) {

- <doc:pattern-prefix-sum>
- <doc:pattern-two-pointers>
- <doc:pattern-sliding-window>
}
